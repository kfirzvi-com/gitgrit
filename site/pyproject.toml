[project]
name = "gitgrit-site"
version = "0.1.0"
description = "GitGrit landing page and documentation"
requires-python = ">=3.13"
dependencies = [
    "mkdocs>=1.6,<2",
    "mkdocs-material>=9.7",
]
