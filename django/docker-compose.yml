services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: gitgud
      POSTGRES_PASSWORD: gitgudpassword
      POSTGRES_DB: gitgud
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - "5432:5432"

volumes:
  postgres_data: